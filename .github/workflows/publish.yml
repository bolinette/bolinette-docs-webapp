name: Publish Bolinette website to server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [14]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node }}
      - name: Install and build
        run: |
          touch .env
          echo API_URL=https://api.docs.bolinette.org >> .env
          echo GITHUB_URL=https://github.com/bolinette/bolinette >> .env
          echo SSH_USERNAME=${{ secrets.PYPI_USERNAME }} >> .env
          echo SSH_PASSWORD=${{ secrets.PYPI_PASSWORD }} >> .env
          npm ci
          npm run build
      - name: Lint
        run: npm run lint
      - name: Deploy
        run: |
          node scripts/deploy.js
